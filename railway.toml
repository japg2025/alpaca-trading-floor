[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildCommand = "docker build --build-arg CACHEBUST=$(date +%s) -t alpaca-trader ."

[deploy]
startCommand = "bash deploy/run_trader.sh"
restartPolicyType = "never"
cronSchedule = "30 14 * * 1-5"
